interface:
  display_name: "媒体理解与模型路由"
  short_description: "无视觉宿主默认用 MiniMax 识图，并路由 OCR、音视频与外部模型"
  default_prompt: "使用 $media-understanding 理解当前媒体；显式调用必须进入本 Skill。若当前宿主/模型不能读取用户本次所附单张图片，用户要求理解即授权该图片默认走 MiniMax；除非用户指定其他 provider 或禁止外发。"

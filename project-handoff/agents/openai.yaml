interface:
  display_name: "Project Handoff Controller"
  short_description: "Plan isolated multi-agent and cross-harness handoffs"
  default_prompt: "Use $project-handoff to plan tasks by file access, workspace mode, and mutable effects, verify each lane's actual workspace and base revision, dispatch Sol, Terra, and Luna only as visible Codex tasks with validated receipts and never as subagents, keep external-harness lanes on standby until launch readback, and keep Spark on its isolated bundled read-only CLI route with terminal failure and no App fallback."

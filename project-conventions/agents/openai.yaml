interface:
  display_name: "Project Conventions"
  short_description: "Initialize exact paths with strict lifecycle boundaries"
  default_prompt: "Use $project-conventions to select full initialization or update-only, initialize only the named target, run initialize_skills_control_project.py when no control project exists, and stop updates after validating the exact checkout."

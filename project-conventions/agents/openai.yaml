interface:
  display_name: "Project Conventions"
  short_description: "Govern multi-repository Skill collections safely"
  default_prompt: "Use $project-conventions to choose the exact lifecycle, distinguish public, private, upstream, and local sources, verify repository mappings and runtime boundaries, and keep update-only scoped to one named package."
